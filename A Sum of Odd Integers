#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() 
{
    int t;
    cin>>t;
    while(t--){
      ll n,k;
      cin>>n>>k;
      if((k*k)>n) cout<<"NO"<<endl;
      else if((n+k)%2!=0) cout<<"NO"<<endl;
      else cout<<"YES"<<endl;
    }
    
}
